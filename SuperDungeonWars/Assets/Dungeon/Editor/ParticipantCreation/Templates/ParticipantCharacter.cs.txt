using System.Collections.Generic;
using UnityEngine;

namespace #PARTICIPANT_NAME#
{
    public sealed class #PARTICIPANT_NAME#Character : ComCharacterBase
    {
        /// <summary>
        /// 毎フレーム（またはゲーム側の思考タイミング）に呼ばれます。
        ///
        /// 個人単位の行動判断が必要な場合に使います。
        /// 基本的な移動先の決定は Party 側に書くことを推奨します。
        ///
        /// 例: 自分の状態に応じた移動停止、個人用の状態更新、
        /// 到達後の次行動の準備など。
        /// </summary>
        protected override void SXG_OnCharacterThink()
        {
        }

        /// <summary>
        /// 攻撃可能な相手から、攻撃する対象の添字を返します。
        ///
        /// attackableCharacters の添字を返すと、その相手を攻撃します。
        /// -1 を返すと攻撃しません。
        ///
        /// 例: 財宝を持つ敵だけを狙う、最も近い敵を狙う、
        /// 自分が財宝を運搬中なら戦闘を避ける、といった方針を書けます。
        /// </summary>
        protected override int SXG_ChooseAttackTargetIndex(
            IReadOnlyList<VisibleCharacterData> attackableCharacters)
        {
            return -1;
        }

        /// <summary>
        /// 財宝に接触したとき、取得するなら true を返します。
        ///
        /// false を返すと、その財宝は拾いません。
        /// たとえば、すでに財宝を持っている場合に拾わない、
        /// 特定種別の財宝だけを拾う、といった方針を書けます。
        /// </summary>
        protected override bool SXG_ShouldPickUpTreasure(TreasureType treasureType)
        {
            return true;
        }

        /// <summary>
        /// 財宝を出口まで運び出した直後に呼ばれます。
        ///
        /// 次の探索目標への切替、搬出回数の記録、
        /// 財宝を運び終えたメンバーの役割変更などに使えます。
        /// </summary>
        protected override void SXG_OnTreasureExported(TreasureType treasureType)
        {
        }

        /// <summary>
        /// キャラクターが設定された目的地セルへ到達したときに呼ばれます。
        ///
        /// 到達を契機に次の行動へ切り替えたい場合に使います。
        /// 移動先を改めて決める処理は、基本的には Party 側の
        /// SXG_OnPartyThink にまとめることを推奨します。
        /// </summary>
        protected override void SXG_OnCellReached(Vector2Int cellPosition)
        {
        }
    }
}